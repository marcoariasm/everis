import ProcedureList from './pages/ProceduresList'
import moduleRouter from './routes';
import ProcedureConstants from './constants';

const components = {
  ProcedureList
}

export default {
  components,
  moduleRouter,
  ProcedureConstants
}